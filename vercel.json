{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install && npm run vercel-build"
      }
    },
    {
      "src": "src/pages/api/**",
      "use": "@vercel/node"
    }
  ]
}
